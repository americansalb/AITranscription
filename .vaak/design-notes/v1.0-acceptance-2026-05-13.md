# Assembly Mode v1.0 Acceptance — Partial Result

Date: 2026-05-13
Status: PARTIAL — Vaak-side validation pending human UI check; AI-side validation deferred to natural session rollover
Spec under test: `.vaak/design-notes/assembly-mode-v1.0-corrected-spec-2026-05-13.md` (with v1.0.2 + v1.0.3 + v1.0.4 + v1.0.5 corrigenda)
Commits under test: 453228c, e582e6e, 1c26267, 7895a03, c687249, af98236, be2b28d, 3e17350, fdae942

## Test plan (per dev-challenger msg 505)

1. project_status returns real assembly state
2. Rule 4 actually halts the floor on substantive yield-to-human
3. Halt/resume audit symmetry on board.jsonl
4. Late-summoner via natural rotation
5. Rule 3a gates AI project_leave during active assembly

## What got verified

### Disk-state verification (UX msg 515)

- `desktop/src-tauri/binaries/vaak-mcp-x86_64-pc-windows-msvc.exe` mtime 2026-05-13T22:49 UTC (post-rebuild for fdae942/v1.0.5).
- Binary contains `halted_for_human`, `floor_resumed_after_human`, `surface_to_next_speaker` strings (UX grep msg 515).
- Architect msg 511 cause (2) ruled OUT (binary IS the rebuild) and cause (3) ruled OUT (file path matches what UX wrote).

### Runtime verification from AI sidecars

- Architect's `mcp__vaak__project_status` call at 22:54 returned the OLD schema — `assembly_active`, `rotation_order`, `current_speaker`, `mic_held_secs` ALL absent.
- Independently reproduced by developer (msg 513) and evil-architect (msg 517).
- Per-role `activity` field from a627daf also absent.

### Root cause (evil-architect msg 517)

Each AI role's MCP sidecar is a separate process spawned by Claude Code at session-start. Vaak desktop app restart only refreshes Vaak's own sidecar, not AI roles' sidecars. AI sessions started ~3h ago are still on the binary that existed at that moment. Architecturally inert from the AI-side observer perspective.

### Result per test step (AI-side)

1. project_status returns real state → **FAIL from old-binary AI-side, PASS via direct protocol.json read** (see below)
2. Rule 4 actually halts → **PARTIAL PASS via direct protocol.json read** (halt flag observed set)
3. Halt/resume audit symmetry → **NOT YET TESTED** — deferred to natural rollover
4. Late-summoner via natural rotation → **PASS observably** (tester:0 in rotation_order via the v1.0.3 append-on-join path running in tester's fresh sidecar)
5. Rule 3a gates AI project_leave → **NOT YET TESTED** — deferred to natural rollover

### Direct-disk verification (architect, 2026-05-13T23:18Z)

After spawning tester:0 (msg 545), read `.vaak/sections/5-12/protocol.json` directly. Observed:

```
floor.rotation_order: [architect:0, developer:0, ux-engineer:0, evil-architect:0,
                       tech-leader:0, ui-architect:0, dev-challenger:0, tester:0]
floor.halted_for_human: true
floor.started_at: 2026-05-13T23:10:39Z
preset: "Assembly Line"
rev: 324
```

Significance:
- **Step 1 underlying data is live**: floor.started_at is a real timestamp (not null); rotation_order is the live 8-seat list; this is exactly the data v1.0.3's migrated `read_assembly_state` reads when constructing the `project_status` response.
- **Step 2 enforcement field is live**: `floor.halted_for_human: true` is set by v1.0.4's code path; the new binary IS writing this primitive. Tester's yield to human earlier (msg 553) triggered rule 4 in tester's fresh sidecar; the flag was written and persists.
- **Step 4 late-summoner-append is live**: tester:0 is at rotation_order[7]. They joined while assembly was active; the v1.0.3 append-on-join code path (vaak-mcp.rs:5695-5712) fired in tester's fresh sidecar; rotation_order was correctly mutated.

### Watchdog kick of tester:0 (architectural finding)

Tester:0 was watchdog-released twice (msg 564: max_floor_exceeded at 300s; msg 568: floor_stall at 121s idle) while presumed mid-tool-call. The current watchdog uses elapsed wall-clock time only and does NOT consult per-seat heartbeat (`last_alive_at_ms`) or declared activity. Real bug; fix scheduled as v1.0.6.

## What can be validated tonight (without restarting all 7 AI sessions)

Human runs the Vaak desktop app (which DID load the new binary on full restart) and visually checks:

- [ ] [YOUR TURN] body no longer renders `Ask:` / `Expected:` lines on routine sends (only when `yield_to.surface_to_next_speaker == true` is set explicitly)
- [ ] Rotation header in [YOUR TURN] body shows full rotation with per-seat activity
- [ ] If Vaak surfaces project_status data anywhere (e.g., ProtocolPanel.tsx), the rotation_order / current_speaker / mic_held_secs fields appear

If all three indicators are visible in the Vaak UI, v1.0.3+v1.0.4+v1.0.5 are live in Vaak's sidecar and the patches are real. Full team-side validation comes when AI sessions naturally roll over.

## Architectural finding (folded into multi-writer audit Instance 9)

Binary deployment is per-process; rebuilds don't propagate to running sidecars. AI roles silently call stale code without any version-mismatch signal. Pattern (c) fix: typed-versioned sidecar contracts with client-side version-mismatch detection so AI roles SEE that their sidecar lags rather than silently calling stale code.

## Open work

- Human visual check on the three Vaak UI indicators above
- Full team-side validation deferred to next-session natural rollover
- Pattern (c) typed-versioned sidecar contracts as a v1.5 candidate (alongside the Preset enum + heartbeat unification)
